# Evidence — admission enforcement

**Generated by `scripts/collect-admission-evidence.sh`. Do not edit by hand.**

Captured: 2026-07-30T13:23:02Z
Cluster:  kate-node-01, kubernetes v1.34.10
Kyverno:  reg.kyverno.io/kyverno/kyverno@sha256:0a540e2ddf74d0d2d3d45f9ef248d7dbc96576accdbcc6a2dd7eaff9fea56504

This is the other half of [supply-chain-verification.md](supply-chain-verification.md).
That file proves the pipeline produced a signed, attested artifact. This one
proves a cluster refuses everything else.

Everything below is the verbatim stdout of `scripts/demo.sh`, which is the same
script recorded in [docs/img/demo.gif](../img/demo.gif).

## What each case is testing

| # | Image | Why it is here | Expected |
|---|-------|----------------|----------|
| 1 | our image, by digest | the happy path must still work, or the policy is just a brick | admit |
| 2 | `busybox`, by digest | an ordinary unsigned image | deny |
| 3 | `kyverno`, by digest | really signed, really in Rekor, wrong identity — separates "has a signature" from "has our signature" | deny |
| 4 | our image, by tag | the signature covers a digest; a tag is a mutable pointer to one | deny |

## Captured run

```console

# The policy is in Enforce, and the webhook fails closed.
$ sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf get clusterpolicy verify-provenance -o jsonpath='{.spec.rules[*].verifyImages[*].failureAction}{"\n"}'
Enforce Enforce
$ sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf get clusterpolicy verify-provenance -o jsonpath='{.spec.webhookConfiguration.failurePolicy}{"\n"}'
Fail

# 1/4  Our image, by digest, signed by the release workflow.
$ kubectl apply -f - <<< "pod signed using ghcr.io/pontope/provenancepipeline@sha256:5d4b03eae45558381b0236be7cacf0cf67d33a3711fb50ec8ab84a0efc692ea4"
pod/signed created
$ sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf get pod signed -n provenance-demo -o wide
NAME     READY   STATUS    RESTARTS   AGE   IP               NODE           NOMINATED NODE   READINESS GATES
signed   1/1     Running   0          6s    10.244.220.159   kate-node-01   <none>           <none>

# 2/4  An ordinary public image. Nothing wrong with it — it just is not ours.
$ kubectl apply -f - <<< "pod unsigned using docker.io/library/busybox@sha256:9532d8c39891ca2ecde4d30d7710e01fb739c87a8b9299685c63704296b16028"
Error from server: error when creating "STDIN": admission webhook "mutate.kyverno.svc-fail" denied the request: 

resource Pod/provenance-demo/unsigned was blocked due to the following policies 

verify-provenance:
  deny-unsigned-in-enforced-namespaces: 'failed to verify image docker.io/library/busybox@sha256:9532d8c39891ca2ecde4d30d7710e01fb739c87a8b9299685c63704296b16028:
    .attestors[0].entries[0].keyless: sigstore bundle verification failed: no matching
    signatures found'

# 3/4  Genuinely Sigstore-signed, by the Kyverno release workflow.

#      A signature is not enough. It has to be OUR signature.
$ kubectl apply -f - <<< "pod other-identity using reg.kyverno.io/kyverno/kyverno@sha256:0a540e2ddf74d0d2d3d45f9ef248d7dbc96576accdbcc6a2dd7eaff9fea56504"
Error from server: error when creating "STDIN": admission webhook "mutate.kyverno.svc-fail" denied the request: 

resource Pod/provenance-demo/other-identity was blocked due to the following policies 

verify-provenance:
  deny-unsigned-in-enforced-namespaces: 'failed to verify image reg.kyverno.io/kyverno/kyverno@sha256:0a540e2ddf74d0d2d3d45f9ef248d7dbc96576accdbcc6a2dd7eaff9fea56504:
    .attestors[0].entries[0].keyless: sigstore bundle verification failed: no matching
    signatures found'

# 4/4  Our image, our signature, referenced by tag instead of digest.

#      The signature covers a digest. A tag is a mutable pointer to one.
$ kubectl apply -f - <<< "pod tagged using ghcr.io/pontope/provenancepipeline:latest"
Error from server: error when creating "STDIN": admission webhook "validate.kyverno.svc-fail" denied the request: 

resource Pod/provenance-demo/tagged was blocked due to the following policies 

verify-provenance:
  deny-unsigned-in-enforced-namespaces: missing digest for ghcr.io/pontope/provenancepipeline:latest
  verify-own-images: missing digest for ghcr.io/pontope/provenancepipeline:latest

# The provenance the cluster just enforced, read back from the registry:
$ cosign verify-attestation --type slsaprovenance1 \
    --certificate-identity 'https://github.com/PontoPe/ProvenancePipeline/.github/workflows/release.yml@refs/heads/main' \
    --certificate-oidc-issuer 'https://token.actions.githubusercontent.com' \
    ghcr.io/pontope/provenancepipeline@sha256:5d4b03eae45558381b0236be7cacf0cf67d33a3711fb50ec8ab84a0efc692ea4 2>/dev/null \
  | jq -r '.payload | @base64d | fromjson | .predicate
      | {buildType: .buildDefinition.buildType,
         builder: .runDetails.builder.id,
         invocation: .runDetails.metadata.invocationId}'
{
  "buildType": "https://github.com/PontoPe/ProvenancePipeline/build-types/github-actions/v1",
  "builder": "https://github.com/PontoPe/ProvenancePipeline/.github/workflows/release.yml@refs/heads/main",
  "invocation": "https://github.com/PontoPe/ProvenancePipeline/actions/runs/30470283063/attempts/1"
}
{
  "buildType": "https://actions.github.io/buildtypes/workflow/v1",
  "builder": "https://github.com/PontoPe/ProvenancePipeline/.github/workflows/release.yml@refs/heads/main",
  "invocation": "https://github.com/PontoPe/ProvenancePipeline/actions/runs/30470283063/attempts/1"
}

# One pod running. Three refused, before anything was pulled or executed.
$ sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf get pods -n provenance-demo
NAME     READY   STATUS    RESTARTS   AGE
signed   1/1     Running   0          13s
```

## What this does and does not prove

- **Does** prove the cluster denies an unsigned image, denies an image signed by
  a different keyless identity, and denies a mutable tag — in `Enforce`, with
  `failurePolicy: Fail`, with nothing switched off to make it pass.
- **Does** prove both halves of the identity pin are load-bearing. Corrupting
  the subject or the issuer alone turns case 1 from admitted into denied; that
  was run separately and is recorded in `docs/PROGRESS.md` step 2.
- **Does not** prove the source was benign. Provenance says where an artifact
  came from, never that what came from there was safe. Threat model T3.
- **Does not** cover every namespace. Rule 2 is opt-in by namespace label, and
  `kube-system`, `kyverno`, `calico-system` and `tigera-operator` are excluded
  from rule 1. Both are enumerated and argued in `policies/kyverno/verify-images.yaml`.
- **Note on the last command:** `cosign verify-attestation` returns *two* SLSA
  provenance documents, one written by `cosign attest` and one by
  `actions/attest-build-provenance`. Kyverno can only discover the second of
  them on GHCR. That asymmetry is the subject of ADR-009 and is visible in the
  output above rather than hidden.
